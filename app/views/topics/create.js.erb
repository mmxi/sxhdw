<% @topic.reload %>
$.facebox.close();
$(document).scrollTop(0);
$('div.topics').prepend("<%= escape_javascript render(:partial => @topic) %>");
$('#topic-<%= @topic.id %>').effect("highlight", {color:"#b1b1b1"}, 3000);